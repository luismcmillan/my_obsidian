[[Python]]
```Python
# Erstellen einer Liste
numbers = [1, 2, 3, 4, 5]

# Zugriff auf Elemente
print(numbers[0])  # Ausgabe: 1

# Hinzufügen eines Elements
numbers.append(6)
print(numbers)  # Ausgabe: [1, 2, 3, 4, 5, 6]

# Entfernen eines Elements
numbers.remove(3)
print(numbers)  # Ausgabe: [1, 2, 4, 5, 6]

```