[[GIT]]
```Shell
git branch name-of-branch
git merge name-of-branch
git branch #shows which branches you have
git checkout branch-name #switches to branch-name
git remote add origin git@github.com:Benutzername/Repository.git
git remote -v //lists all remote addresses
git push origin main -u #pushes local repository to remote repository on branch main
git reset --hard 
```
