- [[Shell]]
```Shell
sudo #Admin Rights
*programm* #starts the programm
rmdir directory_name #deletes empty directory
rm -r directory_name # recursive delete -f (force)
cd # Change Directory
cd .. # change to parent folder
ls # List items
touch filename # erstellt Datei
rm filemane # deletes File
open filename # opens file in standard texteditor
mkdir # erstellt Ordner
clear # removes all statements
pwd # Print working directory
"option" # Click where you want your curser
"control"+C # Cancel
sudo lsof -i -P -n | grep LISTEN # Alle verwendeten Ports auflisten
printenv #Umgebungsvariablen

```

![[Linux Commands]]